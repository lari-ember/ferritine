# Variáveis de Ambiente - Ferritine
# Copie este arquivo para .env e ajuste conforme necessário

# Python Path (normalmente não é necessário se você usar `pip install -e .`)
# PYTHONPATH=.

# Configurações de Log (futuro)
# LOG_LEVEL=INFO
# LOG_FILE=ferritine.log

# Configurações de Simulação (futuro)
# SIM_SPEED=1.0
# SIM_HOURS=24

# Configurações de Hardware/IoT (futuro)
# ARDUINO_PORT=/dev/ttyUSB0
# RASPBERRY_HOST=192.168.1.100

# Configurações de Banco de Dados - PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ferritine
DB_USER=ferritine_user
DB_PASSWORD=ferritine_pass
DB_ECHO=False

# Configurações de Banco de Dados - SQLite (alternativa para desenvolvimento)
SQLITE_PATH=data/db/ferritine.db

# Configurações do PgAdmin (Docker)
PGADMIN_EMAIL=admin@ferritine.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Configurações da Aplicação
LOG_LEVEL=INFO
PYTHONPATH=.


